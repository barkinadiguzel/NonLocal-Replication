# NonLocal-Replication
NonLocal-Replication is a minimal implementation of non-local neural network blocks, focusing on structure rather than training. It demonstrates how each position’s response is computed as a weighted sum of all positions to capture long-range dependencies efficiently.
